/*
 * This header contains general function prototypes (unrelated to the kernel), variables and macros.
 */

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

size_t strlen(const char *str);
